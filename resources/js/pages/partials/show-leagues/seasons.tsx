import { League } from '@/types/match';
import { PropsWithChildren } from 'react';

export default function Seasons({ ...props }: PropsWithChildren<League>) {
    return (
        <>
            <h1>Seasons</h1>
        </>
    );
}
